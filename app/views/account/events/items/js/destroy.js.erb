var element = ".event-item[data-id=<%= id %>]";
toggleModal("#confirmModal", "close");		 
$(element).velocity("fadeOut", {complete: function(){$(element).remove()}});

createAlert({
	type: "success",
	msg: "The event item was deleted",
	timeout: 1000
});